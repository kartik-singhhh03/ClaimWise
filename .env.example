# Environment variables for ClaimWise (copy to .env and fill values)
# NEVER commit real secrets to git. .env is ignored by .gitignore.

# Google Gemini API key for backend LLM features
GEMINI_API_KEY=

# Optional: override Gemini model (backend discovers supported models automatically)
# GEMINI_MODEL=gemini-1.5-flash

# Frontend dev: API base URL (vite)
# VITE_API_BASE_URL=http://localhost:8000
